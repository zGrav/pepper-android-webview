<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="fill_parent"
    android:layout_height="fill_parent" tools:context=".MainActivity" android:id="@+id/activity_main_layout">

    <WebView
        android:id="@+id/activity_main_webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/editText"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/activity_main_webview"
        android:layout_alignEnd="@+id/activity_main_webview"
        android:inputType="textUri"
        android:imeOptions="actionSend"
        android:hint="URL with http(s):// please, otherwise restart the app! :)"/>
</RelativeLayout>
